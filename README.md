# 12practical

import java.util.Random;
import java.util.Scanner;
public class main

public int mcsOn3(int mcsOn3(X)){
    int maxofar=0;
    for(int low=0;low<=X.length;++low){
        int sum=0;
        for(){
            sum+=X[r];
            if(sum>maxofar);
            maxofar=sum;
        }
        return maxofar;



    }
}
